<?php
/**
 * @file
 * Simple Voting module procedural hooks.
 */

/**
 * Implements hook_install().
 */
function simple_voting_install() {
  \Drupal::configFactory()->getEditable('system.site')
    ->set('page.front', '/voting-question')
    ->save();
}